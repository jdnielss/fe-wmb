const dataTable = {
    table: []
}


function addTable (state=dataTable, action) {
    switch (action.type) {
        case 'ADD_NEW_TABLE':
                return {
                    ...state,
                    
                }
            break;
    
        default:
            break;
    }
}